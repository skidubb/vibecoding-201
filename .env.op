# 1Password secret references — resolved at run time by `op run`.
# This file contains NO secrets, only pointers. Safe to commit.
#
#   npm run gen:media
#
# Requires the 1Password CLI (`op`) and an active session (`op signin`).
AI_GATEWAY_API_KEY=op://Dev/Vercel AI Gateway/credential
